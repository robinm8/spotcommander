<?xml version="1.0" encoding="utf-8"?>

<resources>

    <!-- Webview -->
    <style name="webview_webview" parent="@style/webview_webview_base">
        <item name="android:scrollbarThumbVertical">@drawable/scrollbar</item>
        <item name="android:scrollbarSize">6dp</item>
    </style>
    
    <!-- Scrollviews -->
    <style name="scrollview_base">
        <item name="android:scrollbarThumbVertical">@drawable/scrollbar</item>
        <item name="android:scrollbarSize">6dp</item>
    </style>
    
    <!-- Listviews -->    
    <style name="listview_base" parent="android:Widget.Holo.Light.ListView">
        <item name="android:scrollbarThumbVertical">@drawable/scrollbar</item>
        <item name="android:scrollbarSize">6dp</item>
    </style>

    <!-- Progress bar -->
    <style name="progressbar_base" parent="android:Widget.Holo.ProgressBar.Large" />

</resources>